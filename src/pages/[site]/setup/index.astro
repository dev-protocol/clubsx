---
import Layout from '@layouts/Landing.astro'
import PublishForm from '@components/PublishForm/PublishForm.svelte'

const { site } = Astro.params
---

<Layout connectButton="show">
  <PublishForm client:only="svelte" domain={site} />
</Layout>
