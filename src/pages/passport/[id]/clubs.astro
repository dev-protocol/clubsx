---
import UserClubs from '../components/UserClubs.svelte'
import Layout from '@layouts/Landing.astro'
import { isAddress } from 'ethers'

const { id } = Astro.params
---

<Layout connectButton="show" theme="auto">
  <div class="max-w-screen-lg mx-auto px-4 grid gap-24">
    <h2 class="font-body text-5xl font-bold text-white">Clubs</h2>

    <UserClubs client:load id={id ?? ''} />
  </div>
</Layout>
