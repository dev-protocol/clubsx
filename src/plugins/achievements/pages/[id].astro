---
import Achievement_ from '../components/Achievement.svelte'
import { getIdFromURL } from '../utils'

const id = getIdFromURL(Astro.url)
---

<Achievement_ client:only="svelte" achievementId={id || ''} />
