---
import JoinCheckout from '@components/Join/JoinCheckout.vue'

const { amount, propertyAddress, currency, rpcUrl } = Astro.props
---

<JoinCheckout
  client:only
  amount={amount}
  destination={propertyAddress}
  currency={currency}
  page="JOIN"
  rpcUrl={rpcUrl}
/>
